**Unreleased**

* - Enabled TLS certificate verification by default for BigFix API requests.
* - Validated site types and encoded site names before constructing BigFix request paths.
* - Rejected XML document type declarations and bounded streamed response bodies to 5 MiB.
* - Rejected quote characters in hostnames and encoded the complete relevance query parameter.
